export declare enum Combinator {
    DESCENDANT = 1,
    CHILD = 2,
    ADJACENT_SIBLING = 3,
    GENERAL_SIBLING = 4,
    SCOPE = 5
}
export declare function parseCombinator(combinator: string | undefined | null, pattern: string): Combinator;
